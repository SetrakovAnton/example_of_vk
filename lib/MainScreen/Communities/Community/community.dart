import 'package:flutter/material.dart';

class CommunityScreen extends StatelessWidget {
  final int id;
  CommunityScreen({Key? key,required this.id}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('asda'),
      ),
    );
  }
}
